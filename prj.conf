CONFIG_LOG=y
# TEMPORARY: immediate mode, needed while the RX loop polls without
# yielding. Deferred mode must come back before ranging — synchronous
# printing blocks the calling thread for milliseconds and will wreck
# the timestamps.
CONFIG_LOG_MODE_IMMEDIATE=y
CONFIG_LOG_BACKEND_UART=y

CONFIG_SPI=y
CONFIG_GPIO=y
